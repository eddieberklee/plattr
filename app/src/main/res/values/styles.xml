<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="ImagePickerButtonStyle">
        <item name="android:background">@color/flatui_blue_1</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/foreground_background_button_text_size</item>
    </style>

</resources>
